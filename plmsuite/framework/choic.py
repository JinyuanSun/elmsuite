from plmsuite.framework.sequence import Sequence
from plmsuite.framework.embedding import Embedding

class Choice:
    def __init__(self):
        self.sequence = Sequence()
        self.embedding = Embedding()