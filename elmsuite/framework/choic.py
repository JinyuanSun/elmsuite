from elmsuite.framework.sequence import Sequence
from elmsuite.framework.embedding import Embedding


class Choice:
    def __init__(self):
        self.sequence = Sequence()
        self.embedding = Embedding()
